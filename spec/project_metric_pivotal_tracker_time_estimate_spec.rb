require "spec_helper"

describe ProjectMetricPivotalTrackerTimeEstimate do
  it "has a version number" do
    expect(ProjectMetricPivotalTrackerTimeEstimate::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
